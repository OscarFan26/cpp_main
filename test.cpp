#include <bits/stdc++.h>

int get_priority(char ch) {
  if (ch == '*' || ch == '/') return 1;
  if (ch == '+' || ch == '-') return 0;
  if (ch == '(') return -1;
}

int main() {
  std::string input; std::cin >> input;
  std::string ouput;
  std::stack<char>;
  return 0;
}

